<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.home.NotificationFragment">

    <data>

        <variable
            name="vm"
            type="com.umc.presentation.ui.home.NotificationFragmentViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/neutral100"
        android:paddingTop="64dp"
        android:paddingBottom="32dp"
        >

        <!-- 상단 바 -->
        <ImageView
            android:id="@+id/notification_btn_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:src="@drawable/ic_back"
            android:tint="@color/neutral800"
            android:onClick="@{()->vm.onClickBackPressed()}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            />

        <TextView
            android:id="@+id/notificiation_tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/Title2Bold"
            android:textColor="@color/neutral800"
            android:text="@string/home_notification_title"
            android:layout_marginStart="16dp"
            app:layout_constraintTop_toTopOf="@id/notification_btn_back"
            app:layout_constraintBottom_toBottomOf="@id/notification_btn_back"
            app:layout_constraintStart_toEndOf="@id/notification_btn_back"
            />


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/notification_recyclerview"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="32dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/notificiation_tv_title"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:listitem="@layout/item_home_notification"
            />


    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>