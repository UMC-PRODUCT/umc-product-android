<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.mypage.mypost.MypostFragment">

    <data>

        <variable
            name="vm"
            type="com.umc.presentation.ui.mypage.mypost.MypostViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="64dp"
        android:paddingHorizontal="16dp"
        android:background="@color/neutral100"
        >

        <!-- 상단 바 -->
        <ImageView
            android:id="@+id/mypost_btn_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_back"
            app:tint="@color/neutral800"
            app:layout_constraintTop_toTopOf="@id/mypost_tv_title"
            app:layout_constraintBottom_toBottomOf="@id/mypost_tv_title"
            app:layout_constraintStart_toStartOf="parent"
            />

        <TextView
            android:id="@+id/mypost_tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/Title2Bold"
            android:textColor="@color/neutral800"
            android:layout_marginStart="8dp"
            android:layout_marginTop="18dp"
            android:text="@string/mypost_title"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/mypost_btn_back"
            />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/mypost_rcv"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="24dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/mypost_tv_title"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:listitem="@layout/item_mypage_content"/>


    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>